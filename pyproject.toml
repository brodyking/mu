[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
ms = "ms.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ms/"]

[project]
name = "ms"
version = "0.0.1"
requires-python = ">=3.14.0"
description = "Based Music Server"
dependencies = [
    "mutagen",
    "pyside6"
]
